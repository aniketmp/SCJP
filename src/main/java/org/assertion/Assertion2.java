package org.assertion;

public class Assertion2 {
	//Prior to java 1.4 , assert was not keyword.In java 7, by default assert is keyword.
	//Hence if you have old code and in order to compile it ,you have to explicitly tell the 
	//compiler that you are giving it an old piece of code to compile and that it should pretend to be an old compiler.
	//To make it work like prior to 1.4 ways where assert is identifier and not keyword, we can type
	//javac -source 1.3 Assertion2.java
	public static void main(String[] args) {		
//		String assert="hello";
		
	}

}
