package org.collection;

public class ListInfo {
	/* 
	 * List cares about the index. The one thing that List has that non-list don't is a set of methods related to index.
	 * Those key methods include things like get(index), indexOf(Object) ,add(index,object) and so on. You can insert 
	 * an object in the index either by setting an object at particular index or by adding it without specifying position, 
	 * in which case the object is added to the end. List supports duplicates.
	 */

}
